name: SAST

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  sast:
    runs-on: ubuntu-latest
    env:
      SEMGREP_SEND_METRICS: off
      SEMGREP_ENABLE_VERSION_CHECK: "0"
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest certifi
          pip install semgrep
          pip install checkov

      - name: Install Gitleaks
        run: |
          GITLEAKS_VERSION=8.21.2
          curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -o /tmp/gitleaks.tar.gz
          tar -xzf /tmp/gitleaks.tar.gz -C /usr/local/bin gitleaks
          gitleaks version

      - name: Validate and test custom SAST rules
        run: |
          set -e
          mkdir -p .claude/sast/results
          python3 .claude/skills/sast-scan/tools/test_rules.py \
            --rules-dir .claude/skills/sast-scan/rules/semgrep \
            --test \
            --coverage-report .claude/sast/results/rule-coverage.json
          test -f .claude/sast/results/rule-coverage.json || {
            echo "ERROR: rule-coverage.json was not generated — rules test failed"
            exit 1
          }

      - name: Run full test suite
        run: pytest -q tests/

      - name: Run SAST scan
        run: |
          python3 .claude/skills/sast-scan/tools/sast_runner.py \
            --target . \
            --profile standard \
            --format all \
            --fail-on high

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: .claude/sast/results/merged.sarif

      - name: Upload scan artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sast-results
          path: .claude/sast/results/
          retention-days: 30
