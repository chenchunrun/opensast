# ruleid: java.security.spring-actuator-exposed
management.endpoints.web.exposure.include: "*"

# ok: java.security.spring-actuator-exposed
management.endpoints.web.exposure.include: "health,info"

# ruleid: java.security.spring-debug-mode
spring.jpa.show-sql: true

# ok: java.security.spring-debug-mode
spring.jpa.show-sql: false
